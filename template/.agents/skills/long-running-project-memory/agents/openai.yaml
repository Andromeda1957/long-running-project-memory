interface:
  display_name: "Long-Running Project Memory"
  short_description: "Scalable memory for long-lived projects"
  default_prompt: "Use $long-running-project-memory to design or repair a project memory system with runbooks, handoffs, topic memory, deferred plans, repo history, and history separation."
