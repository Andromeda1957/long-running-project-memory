# Changelog

Append-only history of completed work. Newest entries on top. This file is
history; it never overrides current state in `AGENT_HANDOFF.md` or
`docs/memory/`.

## Unreleased

- Adopted the long-running-project-memory system scaffold.
