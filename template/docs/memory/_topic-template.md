# <Topic> Memory

> Copy this file to `docs/memory/<topic>.md`, fill in the sections, and add the
> topic to the index in `PROJECT_MEMORY.md`. Keep `Current` sections operative;
> move outdated facts into `Deprecated / Historical` instead of deleting them.

## Current Setup

- How this part of the system is currently built and configured.

## Current Rules

- Durable rules, conventions, and gotchas that apply to this topic right now.

## Runbooks

- Links to the `docs/runbooks/*.md` workflows relevant to this topic.

## Lookup Anchors

- Key files, paths, commands, or identifiers an agent should grep/inspect for
  implementation truth.

## Deprecated / Historical

- Outdated facts kept for context. Non-operative; does not override `Current`
  sections above.
- For large retired context, link to a specific file under `docs/repo_history/`
  instead of keeping the full archive in this topic file.
